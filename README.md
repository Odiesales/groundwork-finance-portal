# Groundwork Coffee Roasters Finance Portal

Clean rebuild with one shared navigation shell, one branded header system, consistent currency formatting, and preserved AR/revenue data logic.

Run with:

```powershell
py -m streamlit run app.py
```
